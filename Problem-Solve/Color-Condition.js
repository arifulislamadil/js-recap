const answer = false;

const useColor = answer ? "#55343" : "#00000";
console.log(useColor);


const makeUpperCase =(b)=>{
const upperCase = b.toUpperCase()
return upperCase;
}

console.log(makeUpperCase("hello world"));